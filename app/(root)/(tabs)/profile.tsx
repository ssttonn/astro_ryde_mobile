import { SafeAreaView, Text } from "react-native";

const ProfileScreen = () => {
  return (
    <SafeAreaView className="flex-1 bg-black items-center justify-center">
      <Text className="text-primary-100">Profile</Text>
    </SafeAreaView>
  );
};

export default ProfileScreen;
